<?xml version="1.0" encoding="UTF-8"?>
<!--
  Human-readable logging: console plus a rolling file. Containers should instead set
      LOGGING_CONFIG=classpath:log4j2-json.xml

  Every line carries the correlation id supplied by the ledger service, which is what allows a
  screening decision here to be matched to the transfer that asked for it.
-->
<Configuration status="WARN" monitorInterval="60">

    <Properties>
        <Property name="LOG_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%t] [%X{correlationId}] %c{1.} - %msg%n%throwable
        </Property>
        <Property name="LOG_DIR">${sys:LOG_DIR:-${env:LOG_DIR:-logs}}</Property>
        <Property name="APP_NAME">compliance-service</Property>
    </Properties>

    <Appenders>

        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <RollingFile name="RollingFile"
                     fileName="${LOG_DIR}/${APP_NAME}.log"
                     filePattern="${LOG_DIR}/${APP_NAME}-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="20 MB"/>
            </Policies>
            <!-- Bounded: logs must never be the thing that fills the disk and stops the service. -->
            <DefaultRolloverStrategy max="10">
                <Delete basePath="${LOG_DIR}" maxDepth="1">
                    <IfFileName glob="${APP_NAME}-*.log.gz"/>
                    <IfLastModified age="30d"/>
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>

    </Appenders>

    <Loggers>
        <!-- Screening decisions are logged here; every rejection is a WARN with its reason. -->
        <Logger name="com.natwest.compliance" level="DEBUG"/>

        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Root>
    </Loggers>

</Configuration>
